{
  "name": "my-clicky-game",
  "private": true,
  "scripts": {
    "build": "turbo build --no-cache --",
    "dev": "turbo dev --no-cache --",
    "lint": "turbo lint --no-cache --",
    "type-check": "turbo type-check --no-cache --",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "turbo": "latest",
    "typescript": "5.3.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
