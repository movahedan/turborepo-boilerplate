{
  "name": "@repo/web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --max-warnings 0",
    "type-check": "tsc --project ./tsconfig.json",
    "storybook": "storybook dev -p 6201 --quiet",
    "storybook-build": "storybook build -o dist/build/storybook"
  },
  "dependencies": {
    "@repo/ui": "*"
  },
  "devDependencies": {
    "@next/bundle-analyzer": "^14.2.2",
    "@repo/config-eslint": "*",
    "@repo/config-jest": "*",
    "@repo/config-tailwind": "*",
    "@repo/config-typescript": "*"
  }
}
